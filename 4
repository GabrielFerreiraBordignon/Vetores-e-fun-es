#4- Armazene em um vetor todos os números primos entre 1 e 20. Imprima o vetor.
numeros=[]
#percorre todos os numeros de 1 até 20
for numero in range(1,21):
    #a principio, todo numero é primo
    ehPrimo=True
    #for de crescente de numero-1 até 1
    for testaPrimo in range(numero-1,1,-1):
        #testa se ´e divisivel por outro numero
        if(numero%testaPrimo==0):
            #não é primo
            ehPrimo=False
            #sai do for
            break;
        #confirma se numero é primo
        if(ehPrimo==True):
            #adiciona ao vetor
            numeros.append(numero)

print(numeros)
